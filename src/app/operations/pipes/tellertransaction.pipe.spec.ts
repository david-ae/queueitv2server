import { TellertransactionPipe } from './tellertransaction.pipe';

describe('TellertransactionPipe', () => {
  it('create an instance', () => {
    const pipe = new TellertransactionPipe();
    expect(pipe).toBeTruthy();
  });
});
