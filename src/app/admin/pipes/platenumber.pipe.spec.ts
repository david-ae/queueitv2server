import { PlatenumberPipe } from './platenumber.pipe';

describe('PlatenumberPipe', () => {
  it('create an instance', () => {
    const pipe = new PlatenumberPipe();
    expect(pipe).toBeTruthy();
  });
});
